[tool.poetry]
name = {{ project_name }}
version = "0.1.0"
description = "A napalm project with custom analysis modules"
authors = []
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pydantic-sarif = "^0.1.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
